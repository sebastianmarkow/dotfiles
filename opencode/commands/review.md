---
description: Review recent changes or a specific file
agent: review
---

Review the following for correctness, security, and logic errors:

$ARGUMENTS

If no specific files or scope is mentioned, run `git diff` to get all uncommitted changes as the review scope. For each finding, reference the specific file and line number, and explain why it's a problem.
